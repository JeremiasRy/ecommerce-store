const darkCss = `:root { 
    background-color: #fefefe;
    filter: invert(100%);
}
* { 
    background-color: inherit;
}
.div-bg-img {  
    filter: invert(100%);
}
h1, h2, h3, h4 {
    background-color: transparent;
}`;

export default darkCss;